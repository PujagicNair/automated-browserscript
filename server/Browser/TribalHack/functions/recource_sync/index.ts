import { TribalHack } from "../../index";

export function run(hack: TribalHack, pluginOptions: any) {

}

export const meta = {
    name: 'recource-sync',
    description: 'See your recources realtime in the browser',
    config: ['switch-screen']
}