import { TribalHack } from "../../index";
import { IMeta } from "../../IMeta";

export function run(hack: TribalHack, pluginOptions: any) {

}

export const meta: IMeta = {
    name: 'screen-shot',
    description: 'Takes a screenshots of your village every tick',
    addition: 'consider to activate the "load image" options for a better look',
    config: []
}