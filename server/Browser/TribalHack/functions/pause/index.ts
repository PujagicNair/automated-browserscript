import { TribalHack } from "../../index";

export function run(hack: TribalHack, pluginOptions: any) {

}

export const meta = {
    name: 'pause',
    description: 'If you want to play with the browser you have to pause the script before you can log in, because the game cannot handle 2 connections from different locations at the same time',
    config: []
}